---
const { class: className, top = false, ...rest } = Astro.props

const Comp = top ? "header" : "section"
---

<Comp class:list={[top && "pt-nav-height", "py-gy", className]} {...rest}>
  <slot />
</Comp>
